@model ErrorViewModel
@{
    ViewData["Title"] = "Fehler";
}
<style>
    .error-container {
        height: 80vh;
        display: flex;
        justify-content: center;
        align-items: center;
        background: #f6f8fb;
    }

    .error-card {
        background: white;
        padding: 50px;
        border-radius: 20px;
        text-align: center;
        max-width: 500px;
        width: 100%;
    }

    .error-icon {
        font-size: 50px;
        margin-bottom: 15px;
    }

    .error-title {
        font-weight: 700;
        margin-bottom: 10px;
    }

    .error-text {
        color: #6c757d;
        margin-bottom: 25px;
    }

    .error-actions {
        display: flex;
        gap: 10px;
        justify-content: center;
    }

    .error-id {
        margin-top: 20px;
        font-size: 13px;
        color: #999;
    }
</style>
<div class="error-container">

    <div class="error-card shadow-lg">

        <div class="error-icon">⚠</div>

        <h1 class="error-title">Ups, etwas ist schiefgelaufen</h1>

        <p class="error-text">
            Es ist ein unerwarteter Fehler aufgetreten.<br />
            Bitte versuchen Sie es erneut oder kontaktieren Sie den Support.
        </p>

        <div class="error-actions">

            <a href="/" class="btn btn-primary">
                Zur Startseite
            </a>

            <button onclick="location.reload()" class="btn btn-outline-secondary">
                Seite neu laden
            </button>

        </div>

        @if (Model?.RequestId != null)
        {
            <div class="error-id">
                Fehler-ID: <strong>@Model.RequestId</strong>
            </div>
        }

    </div>

</div>